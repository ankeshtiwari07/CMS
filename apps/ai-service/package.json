{
  "name": "@humain/ai-service",
  "version": "1.0.0",
  "type": "module",
  "description": "HUMAIN AI service \u2014 Claude generation + local embeddings",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --import tsx --watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file-if-exists=.env dist/server.js",
    "worker": "node --env-file-if-exists=.env --import tsx src/worker.ts",
    "indexer": "node --env-file-if-exists=.env --import tsx src/indexer.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@fastify/rate-limit": "^11.2.0",
    "@xenova/transformers": "^2.17.2",
    "bullmq": "^5.13.0",
    "fastify": "^5.7.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0",
    "pino-pretty": "^11.2.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}

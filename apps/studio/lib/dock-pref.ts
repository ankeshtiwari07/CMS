/** Copilot dock preference. Non-client module: the server frame reads it too
    (see lib/sidebar-pref.ts for why a "use client" module cannot hold this). */
export const DOCK_KEY = "humain-cms-copilot";
